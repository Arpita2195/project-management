{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "TaskFlow – Full-stack MERN Project Management Tool",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build && node -e \"const fs = require('fs'); fs.rmSync('../dist', { recursive: true, force: true }); fs.cpSync('./dist', '../dist', { recursive: true });\"",
    "start": "cd server && node server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "react-big-calendar": "^1.19.4"
  }
}
